#include <stdio.h>
int main()
{
	int n,k,temp;
	scanf("%d",&n);
	int score[101]={0};
	for(int i=0;i<n;i++)
	{
		scanf("%d",&temp);
		score[temp]++;
	}
	scanf("%d",&k);
	for(int i=0;i<k;i++)
	{
		scanf("%d",&temp);
		if(i!=0)
		printf(" ");
		printf("%d",score[temp]);
	}
	return 0;
}
